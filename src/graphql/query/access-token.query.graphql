query AccessToken {
    accessToken {
        currentUser {
            id,
            personId,
            status,
            email,
            firstName,
            lastName,
            role,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken
    }
}