mutation Login($input: LoginInput!) {
    login(loginInput: $input) {
         currentUser {
            id,
            personId,
            status,
            email,
            firstName,
            lastName,
            role,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken
    }
}