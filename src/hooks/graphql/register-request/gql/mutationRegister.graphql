mutation Register($input: CreateUserInput!) {
    register(registerData: $input){
        user{
            id,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken
    }
}
