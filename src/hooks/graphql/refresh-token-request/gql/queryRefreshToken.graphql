query RefreshToken {
    refreshToken{
        user {
            id,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken,
    }
}
