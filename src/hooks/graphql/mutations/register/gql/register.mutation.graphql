mutation Register($input: CreateMemberInput!) {
    register(registerInput: $input){
        payload{
            id,
            userId,
            status,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken
    }
}
