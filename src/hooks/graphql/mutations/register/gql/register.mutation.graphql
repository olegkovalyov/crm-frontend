mutation Register($input: CreateMemberInput!) {
    register(registerData: $input){
        user{
            id,
            status,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken
    }
}
