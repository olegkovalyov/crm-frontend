mutation CreateUser($user: CreateUserInput!){
    createUser(createUserInput: $user){
        id,
        status,
        firstName,
        lastName,
        email,
        role,
        licenseType,
        createdAt,
        updatedAt
    }
}