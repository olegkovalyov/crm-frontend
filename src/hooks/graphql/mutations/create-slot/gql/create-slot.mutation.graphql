mutation CreateSlot($input: CreateSlotInput!) {
    createSlot(createSlotInput: $input){
        id,
        status,
        order,
        date,
        aircraft,
        notes,
        slots {
            id,
            userId,
            firstName,
            lastName,
            role,
            description
        }
    }
}
