mutation CreateLoad($load: CreateLoadInput!){
    createLoad(createLoadInput: $load){
        id,
        status,
        capacity,
        order,
        slots {
            id
        },
        info,
        createdAt,
        updatedAt
    }
}
