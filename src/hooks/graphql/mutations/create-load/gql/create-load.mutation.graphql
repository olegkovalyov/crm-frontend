mutation CreateLoad($input: CreateLoadInput!) {
    createLoad(createLoadInput: $input)
    {
        id,
        status,
        order,
        date,
        aircraft,
        notes,
        slots {
            id,
            userId,
            firstName,
            lastName,
            role,
            description
        }
    }
}
