query GetUsers($getUsersFilter: GetUsersFilterInput!) {
    getUsers(getUsersFilterInput: $getUsersFilter){
        id,
        status,
        firstName,
        lastName,
        email,
        roles,
        licenseType,
        createdAt,
        updatedAt
    }
}
