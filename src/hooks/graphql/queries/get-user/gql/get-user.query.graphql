query GetUser($userId: String!) {
    getUser(id: $userId) {
        id,
        status,
        firstName,
        lastName,
        email,
        roles,
        licenseType,
        createdAt,
        updatedAt
    }
}
