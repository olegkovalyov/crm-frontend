query GetClient($id: Int!) {
    getClient(id: $id) {
        id,
        userId,
        type,
        status,
        gender,
        age,
        firstName,
        lastName,
        email,
        weight,
        phone,
        address,
        withHandCameraVideo,
        withCameraman,
        notes,
        certificate,
        paymentStatus,
        tm {
            id
        },
        cameraman {
            id
        }
        createdAt,
        processedAt
    }
}
