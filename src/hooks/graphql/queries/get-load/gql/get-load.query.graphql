query GetLoad($id: Int!) {
    getLoad(id: $id) {
        id,
        status,
        capacity,
        order,
        slots {
            id
        },
        info,
        takeOffTime,
        landingTime,
        createdAt,
        updatedAt
    }
}