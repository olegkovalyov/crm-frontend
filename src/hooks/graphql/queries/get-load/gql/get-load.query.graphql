query GetLoad($id: Int!) {
    getLoad(id: $id) {
        id,
        status,
        order,
        date,
        aircraft,
        notes,
        slots {
            userId,
            description
        }
    }
}
