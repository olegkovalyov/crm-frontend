mutation Login($input: LoginInput!) {
    login(loginData: $input) {
        user {
            id,
            status,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken,
    }
}
