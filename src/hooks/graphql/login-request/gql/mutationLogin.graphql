mutation Login($input: LoginInput!) {
    login(loginData: $input) {
        user {
            id,
            firstName,
            lastName,
            email,
            roles,
            licenseType,
            createdAt,
            updatedAt,
        },
        accessToken,
    }
}
