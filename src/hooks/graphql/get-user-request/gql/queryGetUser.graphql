query GetUser($input: String!) {
    getUser(id: $input) {
        id,
        status,
        firstName,
        lastName,
        email,
        roles,
        licenseType,
        createdAt,
        updatedAt
    }
}
