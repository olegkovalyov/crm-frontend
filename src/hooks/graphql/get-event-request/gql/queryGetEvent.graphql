query GetEvent($input: String!) {
    getEvent(id: $input) {
        id,
        name,
        date,
        notes,
        loads{
            id,
            aircraft,
            date,
            status
        }
    }
}
