mutation ResetPassword($input: ResetPasswordInput!) {
    resetPassword(resetPasswordData: $input){
        user {
            id,
            firstName,
            lastName,
            email,
            role,
            licenseType
        }
        token
    }
}
