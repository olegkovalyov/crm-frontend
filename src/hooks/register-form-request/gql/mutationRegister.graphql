mutation Register($input: CreateUserInput!) {
    register(registerData: $input){
        user{
            id,
            firstName,
            lastName,
            email,
            role,
            licenseType
        }
        token,
    }
}
