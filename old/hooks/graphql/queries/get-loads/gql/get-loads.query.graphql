query GetLoads($eventId: Int!) {
    getLoads(eventId: $eventId){
        id,
        status,
        capacity,
        order,
        slots {
            id
        },
        info,
        takeOffTime,
        landingTime,
        createdAt,
        updatedAt
    }
}