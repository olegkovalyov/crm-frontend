mutation CreateEvent($event: CreateEventInput!){
    createEvent(createEventInput: $event){
        id,
        name,
        startDate,
        endDate,
        info,
        createdAt,
        updatedAt
    }
}