mutation DeleteLoad($id: Int!){
    deleteLoad(id: $id) {
        id,
        status,
        capacity,
        order,
        slots {
            id
        },
        info,
        createdAt,
        updatedAt
    }
}